/**
 * title: Basic
 * desc: The simplest usage.
 */
import React, { FC } from 'react'
import { Tooltip } from 'sparkd'

const Demo: FC = () => {
  return <Tooltip title='this is basic usage'>Mouse up</Tooltip>
}

export default Demo
